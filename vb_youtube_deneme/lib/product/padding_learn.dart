import 'package:flutter/material.dart';

class PaddingItems {
  static const vertical = EdgeInsets.only(top: 15.0, bottom: 15.0);
  static const horizontal = EdgeInsets.only(left: 15.0, right: 15.0);
  static const all = EdgeInsets.all(15.0);
  static const buttonPadding =
      EdgeInsets.only(right: 40, left: 40, top: 10, bottom: 10);
}
